{
    "name": "ixopay/ixopay-php-client",
    "description": "Ixopay Client for PHP",
    "keywords": [
        "ixopay"
    ],
    "license": "MIT",
    "type": "library",
    "require": {
        "php": ">=v8.1",
        "ext-curl": "*",
        "psr/log": "*"
    },
    "scripts": {
        "phpstan": [
            "./vendor/bin/phpstan analyze -l 0 -- src"
        ]
    },
    "autoload": {
        "psr-4": {
            "Ixopay\\Client\\": "src/"
        }
    },
    "config": {
        "preferred-install": "dist",
        "sort-packages": true
    },
    "minimum-stability": "dev",
    "prefer-stable": true,
    "require-dev": {
        "phpstan/phpstan": "^2.0"
    }
}
